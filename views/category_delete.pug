extends layout 

block content
    h1 #{title}
    p delete category
    p remember deleting this categories will also delete all the toys inside this category
    p #{category_id}

    form(action="", method="post")  
        input(type="hidden" name="name" value=category_id)
        br
        br
        label(for="admin_password") admin password: 
        input(type="text" name="admin_password")
        br
        br

        button(type="submit") delete