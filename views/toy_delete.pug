extends layout 

block content 
    h1 #{title}

    p delete toy
    p #{toy_id}

    form(action="", method="post")  
        input(type="hidden", name="name" value=toy_id)
        br
        br
        label(for="admin_password") admin password: 
        input(type="text" name="admin_password")
        br
        br

        button(type="submit") delete